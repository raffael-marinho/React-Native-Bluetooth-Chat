// Learn more on how to config Metro: https://facebook.github.io/metro/docs/configuration
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;